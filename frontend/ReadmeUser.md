# UserHub

Simple React + Vite frontend for creating, viewing, updating, and deleting users with Tailwind-based styling.

## Setup
- Prerequisites: Node.js (≥18) and npm.
- Install dependencies: `npm install` (run inside `frontend/`).
- Start dev server: `npm run dev` (inside `frontend/`), then open the shown localhost URL.

## Tech Stack
- React 19 + React Router DOM
- Vite
- Tailwind CSS

